# Core
anthropic>=0.69.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9   # FastAPI file uploads

# Transcription (diarization). Only needed when ASR_PROVIDER=assemblyai.
assemblyai>=0.34.0

# Word (.docx) export of the themes/quotes report. Optional — without it the
# app falls back to an HTML document that Word still opens.
python-docx>=1.1.0
